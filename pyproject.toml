[project]
name = "easytrain"
version = "0.0.1"
description = "Testing and CI layer for model behavior — grader IDE + reward-function CI/CD."
requires-python = ">=3.12"
readme = "README.md"

[tool.uv.workspace]
members = [
    "packages/sdk",
    "packages/rft_compile",
    "packages/differ",
    "packages/monitor",
    "packages/mutate",
    "packages/kappa",
    "packages/store",
    "packages/server",
]

[tool.uv.sources]
easytrain-sdk = { workspace = true }
easytrain-rft = { workspace = true }
easytrain-differ = { workspace = true }
easytrain-monitor = { workspace = true }
easytrain-mutate = { workspace = true }
easytrain-kappa = { workspace = true }
easytrain-store = { workspace = true }
easytrain-server = { workspace = true }

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.6",
    "httpx>=0.27",
    "easytrain-sdk",
    "easytrain-rft",
    "easytrain-differ",
    "easytrain-monitor",
    "easytrain-mutate",
    "easytrain-kappa",
    "easytrain-store",
    "easytrain-server",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "N", "S", "RUF"]
ignore = ["S101"]  # allow asserts in tests

[tool.ruff.lint.per-file-ignores]
"**/tests/**" = ["S", "N"]

[tool.pytest.ini_options]
testpaths = [
    "packages/sdk/tests",
    "packages/rft_compile/tests",
    "packages/differ/tests",
    "packages/monitor/tests",
    "packages/mutate/tests",
    "packages/kappa/tests",
    "packages/store/tests",
    "packages/server/tests",
]
addopts = "-ra --strict-markers --import-mode=importlib"
